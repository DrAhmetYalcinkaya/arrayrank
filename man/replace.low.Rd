% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/replace.low.R
\name{replace.low}
\alias{replace.low}
\title{Replace Low Values in Data}
\usage{
replace.low(
  data,
  offset = 10,
  strategy = "computed",
  fix = offset,
  verbose = TRUE
)
}
\arguments{
\item{data}{A data frame containing numeric values, such as protein expression data.}

\item{offset}{A numeric value specifying the threshold below which values will be replaced. Default is 10.}

\item{strategy}{A character string specifying the replacement strategy: \code{"computed"} for a computed value (default), \code{"fixed"} for a fixed replacement value, or \code{"random"} for adding small random noise.}

\item{fix}{A numeric value used when \code{strategy} is \code{"fixed"}. Default is \code{offset}.}

\item{verbose}{A logical value indicating whether to print the number of replacements made. Default is TRUE.}
}
\value{
A data frame with low values replaced based on the specified offset and strategy.
}
\description{
This function replaces values in a data frame that are below a specified offset with a corrected value. Users can choose different strategies for replacing low values.
}
\examples{
\dontrun{
# Replace low values in the data frame with a computed value
corrected_data <- replace.low(data = your_data, offset = 10)

# Replace low values with a fixed value
corrected_data <- replace.low(data = your_data, offset = 10, strategy = "fixed", fix = 5)

# Replace low values with small random noise
corrected_data <- replace.low(data = your_data, offset = 10, strategy = "random")
}
}
